{
  "name": "helios-apphosting",
  "version": "1.0.0",
  "private": true,
  "description": "HELIOS Mission Design — Firebase App Hosting (Next.js SSR shell + client SPA)",
  "scripts": {
    "prebuild": "node scripts/prepare-spa-assets.mjs",
    "dev": "node scripts/prepare-spa-assets.mjs && next dev -p 3000",
    "build": "next build",
    "start": "next start",
    "lint": "echo skip"
  },
  "dependencies": {
    "next": "^15.5.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
