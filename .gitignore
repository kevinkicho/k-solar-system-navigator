node_modules/
tests/screenshots/
tests/.logs
temp_importmap.txt
test.txt
test2.txt
.DS_Store
*.log

# Secrets — never commit API keys / local env
.env
.env.local
.env.*.local
.env.*.private

# Firebase local / CLI noise (firebase.json & .firebaserc are OK to commit)
.firebase/
firebase-debug.log
firebase-debug.*.log
ui-debug.log
*.cache
# Accidental firebase init SPA stub (hosting public is project root ".")
public/
# Service account keys (never commit) — Admin SDK private keys for local agent use only
# e.g. k-solar-system-navigator-firebase-adminsdk-*.json at repo root
*-firebase-adminsdk-*.json
*firebase-adminsdk*.json
serviceAccount*.json
**/service-account*.json
google-services.json
GoogleService-Info.plist
